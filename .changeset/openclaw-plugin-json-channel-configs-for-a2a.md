---
"@aramisfa/openclaw-a2a-inbound": patch
---

Add `channelConfigs.a2a` metadata to the inbound plugin manifest so OpenClaw can expose the channel configuration schema and setup UI before runtime loading.
