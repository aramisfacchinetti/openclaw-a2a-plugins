---
"@aramisfa/openclaw-a2a-outbound": patch
---

Fix `remote_agent` registration during OpenClaw `tool-discovery` passes so the tool is available to agent turns that discover tools without performing full plugin activation.
