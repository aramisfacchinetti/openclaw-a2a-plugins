---
"@aramisfa/openclaw-a2a-outbound": patch
---

Keep `openclaw a2a ... --json` machine-readable by writing command payloads to stdout, reduce default plugin lifecycle log noise during CLI startup, and fix the outbound package README quickstart link on npm so it resolves to the repository guide.
