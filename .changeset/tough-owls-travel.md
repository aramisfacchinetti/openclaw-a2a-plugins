---
'@aramisfa/openclaw-a2a-outbound': major
---

Replace the flat `remote_agent` summary task fields with nested `summary.continuation.task` and `summary.continuation.conversation` objects so task lifecycle and conversation continuity are handled separately.
